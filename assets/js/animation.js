var animation = lottie.loadAnimation({
  container: document.getElementById('anim'),  // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'assets/lottie-anim/createaccount_data.json' // the path to the animation json
});
